//import java.util.LinkedList;
//LinkedList queue=new LinkedList<Float>();
//queue.add(.1);
//queue.add(.2);//add a couple numbers (add adds to the end of the list)
//for (int i=0; i<queue.size(); i++) {
//  println(queue.get(i)); //print all the numbers in the queue
//}
//queue.remove(0);//remove the first value from the front of the queue
//println(queue.get(0));//so now .2 is the first value


//float[] volts = new float[50];
//void runBatGraph() {
//  test++;
//  if (test==628) test=0;
//  batVolt=sin(test/100)+1;


//  for (int i=1; i<volts.length; i++) {
//    volts[i]=volts[i-1];
//  }

//  volts[0]=batVolt;
//  println(volts);
  
//  fill(0,255,0);
//  for (int i=1; i<volts.length; i++) {
//    line(
//  }
//}
